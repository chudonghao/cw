# CW 语言参考手册

本文同时是 CW 的用户参考手册和用户可见语言规范。“必须”“不得”和“允许”分别
表示强制要求、禁止行为和可选行为。本文使用的语法片段只省略与当前规则无关的部分。

## 词法元素

### 空白与注释

空格、水平制表符和换行用于分隔 token，本身不参与程序语义。行注释以 `//` 开始，
一直延续到当前行末尾。

### 标识符与保留字

标识符用于命名类型、变量、函数和字段。与保留字完全相同的拼写不能作为标识符。

CW 使用以下保留字：

```text
trivial struct virtual abstract override func var ctor dtor operator
if else while break continue return this
const mut copy move
void bool i8 i16 i32 i64 u8 u16 u32 u64 f32 f64 isize usize
true false null
```

### 字面量

布尔字面量为 `true` 和 `false`。

空值字面量为 `null`。它具有独立的空值语义类型，不是普通对象，也不具有对象值类别。
该类型没有源码名称，不能直接用作变量、字段、形参或返回对象的类型。`var n := null;`
不能推导出对象类型；局部与全局声明适用相同规则。整数字面量 `0` 保持整数语义，不是
空指针字面量。

整数字面量本身不带正负号，可使用以下进制：

| 进制 | 形式 | 示例 |
|---|---|---|
| 二进制 | `0b` 或 `0B` 后接二进制数字 | `0b1010` |
| 八进制 | `0o` 或 `0O` 后接八进制数字 | `0o755` |
| 十进制 | 十进制数字序列 | `123` |
| 十六进制 | `0x` 或 `0X` 后接十六进制数字 | `0xFF` |

整数字面量表示任意精度的非负整数值。源码中的负整数由一元负号和整数字面量组成。

浮点字面量必须包含小数点，小数点两侧至少一侧包含数字；可以带十进制指数。后缀
`f` 或 `F` 使字面量具有 `f32` 类型，没有该后缀时具有 `f64` 类型：

```cw
1.0
.5
2.e3
1.0f
```

字符字面量使用单引号并表示一个字节，其类型为 `u8`。字符串字面量使用双引号。
字符和字符串支持 `\n`、`\r`、`\t`、`\0`、`\\`、对应引号的转义，以及带一至两个
十六进制数字的 `\x` 转义。

相邻字符串字面量按源码顺序拼接为一个字符串字面量。每个 token 先独立解码，拼接时
不重新解释转义；例如 `"\x4" "1"` 包含字节 `0x04` 和 `0x31`。

### 运算符与分隔符

本文定义的表达式使用以下运算符：

```text
.  ->  move  &
+  -  !
*  /  %
<  <=  >  >=  ==  !=
&&  ||
?  :
:=  =
```

`,` 分隔声明项、形参、实参、数组元素或多结果表达式；`;` 结束声明或语句；`()` 用于
参数列表、调用和分组；`{}` 界定块或数组元素列表；`[]` 用于固定数组类型和后缀下标；
类型中的前缀 `*` 表示指针。

## 类型

### 内建类型

| 类型 | 含义 |
|---|---|
| `void` | 不产生值 |
| `bool` | 布尔值 `false` 或 `true` |
| `i8`、`i16`、`i32`、`i64` | 8、16、32、64 位有符号整数 |
| `u8`、`u16`、`u32`、`u64` | 8、16、32、64 位无符号整数 |
| `f32`、`f64` | 两种浮点类型 |
| `isize`、`usize` | 位宽与目标平台指针相同的有符号和无符号尺寸整数 |

固定宽度整数、尺寸整数和浮点数是三个不同的数值家族。`isize` 与 `usize` 不属于
固定宽度整数等级序列。

裸 `void` 只用于表示函数不返回值，不得作为形参、局部变量、全局变量或具名返回对象
的类型。`void` 仍可嵌套在函数类型或指针类型中，例如 `func () void` 和 `*void`。

### const 限定类型

`const T` 是直接拥有 `T` 对象的限定类型，但禁止通过该对象表达式修改或实际移动它。
`const` 不是值类别，也不建立引用。读取 `const T` 仍可执行 `T` 支持的只读操作；
`const` 只禁止需要修改或移动该对象的操作。
显式 `move` 只改变值类别，允许用于 const 对象左值，后续对象操作仍服从 const 权限。

初始化 `const T` 时，先按 `T` 的普通对象初始化规则完成对象初始化，再施加顶层
`const` 限定。该过程不把源表达式转换成 `const T` 表达式。

`const` 可以与复合类型构造递归组合。它限定紧随其后的完整类型，因此 `*const T`
表示指向 `const T` 的指针，而 `const *T` 表示自身具有 `const` 限定的 `*T` 指针
对象。

`const` 只能限定对象类型，不得限定 `void`、函数类型或引用类型。同一对象层不得重复
限定，因此 `const const T` 非法；不同对象层的限定不重复，例如 `const *const T`
同时限定指针对象和 pointee，仍然合法。

### 编译期整数

普通整数字面量及完全由一元 `+`、`-` 包裹它形成的表达式，在默认定型前表示精确的
任意精度整数值，尚不具有运行时整数类型。源码不能显式声明这种未定型整数形式。

编译期整数需要成为运行时值时，按顺序选择第一个能精确表示其值的类型：

1. `i32`
2. `i64`
3. `u64`

负值不会选择 `u64`。三个类型均不能表示该值时，程序错误。外围声明类型、函数形参
类型或另一操作数的类型都不会改变这一选择。

### 指针类型

`*T` 表示指向 `T` 的原始指针。`T` 是该指针的 pointee type（被指向类型），指针
实际指向的对象称为 pointee（被指向对象）。前缀可以递归组合，例如 `**u8` 是指向
`*u8` 的指针。

指针自身与 pointee 的 `const` 限定分别组合：

| 类型 | 含义 |
|---|---|
| `*T` | 指向可修改 `T` 的指针 |
| `*const T` | 指向不可修改 `T` 的指针 |
| `const *T` | 自身不可修改、指向可修改 `T` 的指针 |
| `const *const T` | 自身和 pointee 均不可修改的指针 |

原始指针值可以安全增加 pointee 的 `const`。直接增加限定时，`*T` 可以隐式转换为
`*const T`。多级指针按每一层 pointee 分解限定：只能增加 `const`；若在更深层新增
`const`，目标中此前经过的每个中间指针对象都必须同时是 `const`，以阻止通过转换结果
回写不兼容指针。因此 `**T` 可以转换为 `*const *T` 和 `*const *const T`，但不能转换为
`**const T`。指针对象自身最外层的顶层 `const` 仍按普通值读取规则处理，不属于该分解。

若 `Derived` 沿唯一静态继承路径派生自 `Base`，`*Derived` 可以隐式转换为 `*Base`；
pointee 的 `const` 必须保留，也可以同时增加，因此 `*const Derived` 可以转换为
`*const Base`，`*Derived` 可以转换为 `*const Base`，但 `*const Derived` 不能转换为
`*Base`。地址调整只作用于当前指针的直接结构体 pointee，不递归转换内部指针；例如
`**Derived` 不能转换为 `**Base`。

`null` 可以直接形成任意合法原始指针、普通函数指针或虚接口指针类型的空值纯右值，
包括多级指针及合法的嵌套限定组合。空值与该指针类型的默认值相同，是已初始化的值，
不能通过它访问对象或调用函数。该规则不规定空值的字节表示或错误访问的处理方式。

```cw
var pointer *i32 := null;
var nested **const i32 := null;
var callback *func () void := null;
```

初始化、赋值、返回和传参均可由目标指针类型形成空值。`null` 不转换为整数、`bool`、
结构体或数组，也不代表组合对象的整体默认初始化。已经定型的指针表达式继续遵循普通
指针转换规则，不因保存空值而获得其它跨类型转换。

### 函数类型

函数类型写为：

```text
func (<参数类型列表>) <返回类型>
```

参数列表只包含类型，返回类型不得省略。类型可以递归嵌套，也可以位于指针之后：

```cw
func () void
func (i32, *S) *S
*func (i32) void
```

函数类型描述函数签名，本身不是对象类型，不得直接用作局部变量、全局变量、按值形参
或返回结果的类型。保存或传递普通函数使用函数指针类型，例如 `*func (i32) void`。

函数类型内部的形参和返回类型遵守相同的使用资格规则；外层指针不改变内部签名的
要求。例如 `*func (*func () void) void` 合法，`*func (func () void) void` 的形参
类型非法，`*func () func () void` 的返回类型非法。

函数类型中的普通按值形参和普通按值返回不得带顶层 `const`，因为该限定只限制被调函数
内的形参对象，或限制已经交给调用方的返回对象，不构成调用者需要观察的接口能力。
嵌套限定仍然属于接口类型：`*const T` 形参或返回合法，`const *T` 形参或返回非法。
引用形参和引用返回不属于普通按值类型，其引用模式完整保留在函数类型中。

### 结构体类型

每个 `struct` 声明建立一个同名结构体类型。结构体类型的身份由声明决定；同名但不同
的声明不能合并为一个类型。

### 固定长度数组类型

固定长度数组类型写为 `[N] T`，表示依次包含 `N` 个 `T` 元素子对象的拥有对象类型。
长度是类型身份的一部分；元素类型或长度不同的数组是不同类型。`N` 按编译期求值结果
归一化，因此 `[2 + 2] T` 与 `[4] T` 是同一类型，源码表达式的具体拼写不参与类型身份。

当前数组长度常量表达式可以由整数字面量、括号、一元 `+`/`-` 以及二元 `+`、`-`、`*`
组成。每个已定型中间结果都必须能够由其整数类型精确表示；最终值必须非负并可由目标
平台的 `usize` 表示。名称、函数调用、除法、余数以及其它表达式当前不能形成数组长度。
零长度合法，`[0] T` 仍是由元素类型和长度共同确定的独立对象类型。

数组元素必须是能够作为完整拥有对象存在的类型。内建对象、原始指针、普通函数指针、
虚接口指针、具体结构体、带合法 `const` 限定的对象和其它数组均可作为元素；`void`、裸
函数、引用和抽象结构体不能作为元素。该资格与长度无关，因此 `[0] Abstract` 仍然非法；
`*Abstract` 可以合法指向基类子对象，但 `*[N] Abstract` 仍因其 pointee 数组含有抽象
元素而非法。

数组是普通对象，不隐式退化为元素指针，也不与元素指针或不同长度数组互相转换。结构体
不得通过字段、直接基类和任意层数组元素按值直接或间接包含自身；零长度数组不切断这种
按值包含关系。指针和引用仍然切断按值包含关系。

当 `N > 0` 时，数组元素按下标递增连续排列，数组与元素 `0` 具有相同起始地址，元素 `i`
的字节偏移为 `i * sizeof(T)`；数组不在相邻元素之间增加额外填充。若元素大小为零，各
元素仍具有不同的语义对象身份，但可以共享地址。对所有长度，
`alignof([N] T) = alignof(T)` 且 `sizeof([N] T) = N * sizeof(T)`；因此
`sizeof([0] T) = 0`。不同零大小对象可以共享地址，语言不以地址相等判断对象身份。
所有完整对象的布局还必须能由目标表示并不超过目标统一的最大对象尺寸；数组以受检查的
`N * sizeof(T)` 参与这项通用规则。实际目标布局与尺寸诊断将由统一类型布局设施实现，
不使用数组专属固定上限。

### 引用类型

referent 是引用所指代的对象。CW 定义三种引用类型构造器：

| 类型形式 | 权限与绑定 |
|---|---|
| `mut T` | 绑定可修改的 `T` 左值；可读取、修改、复制或移动 referent |
| `copy T` | 绑定能够按 `const T` 访问的对象；不得通过该引用修改或实际移动 referent |
| `move T` | 绑定 `T` 移动左值或物化后的 `T` 纯右值；权限与 `mut T` 相同 |

完成初始化的引用必定绑定一个 referent，不存在空引用值；绑定建立后不可重新绑定。
引用表达式透明地表示 referent，引用本身没有独立的普通对象表达式。绑定引用本身不
复制或移动 referent；真正的复制或移动只在普通对象的构造或赋值中发生。`copy T` 不
要求 `T` 必须支持复制；只有实际执行复制时才要求相应操作存在。

CW 不提供独立的引用值构造表达式，也不提供可由普通表达式操作的引用值。初始化引用时，
直接以符合引用模式的普通对象表达式作为来源建立绑定。

`mut T` 可以用于局部引用变量；`mut T`、`copy T` 和 `move T` 都可以用于形参和返回
类型。引用模式可以作用于指针对象，例如 `mut *T` 是可修改引用一个 `*T` 指针对象。
指针不得以引用类型为 pointee，引用模式也不得嵌套。

`mut const T` 和 `move const T` 非法，因为它们无法提供各自承诺的操作权限；
`copy const T` 与 `copy T` 语义重复，也属于非法冗余拼写。`const` 不得限定引用本身。

## 声明与作用域

### 翻译单元

翻译单元由零个或多个顶层声明组成。顶层可以声明结构体、普通函数、构造函数、析构
函数和变量组。

顶层结构体、普通函数和全局变量共享普通名称空间。结构体或变量名称必须唯一；同名
普通函数可以形成重载集合。顶层类型和函数可以前向引用；各函数体均能引用完整翻译
单元中的全局变量。全局初始化按声明顺序分析，只能直接引用已进入作用域的全局变量，
不能直接访问后声明变量。函数定义的位置不表示函数体的执行时机。

### 词法作用域

函数形参、具名返回对象和函数体最外层的局部变量处于同一作用域。每个嵌套块建立一个
内层作用域。

同一作用域不得重复声明同名变量。内层局部变量可以与外层局部变量同名；在内层变量的
作用域内，外层同名局部变量不可见，离开该作用域后重新可见。

使用表达式列表初始化全局或局部变量组时，组内名称在全部初始化表达式完成后才进入作用域，
因此初始化表达式不能引用自身或同组变量。变量初始化块采用不同规则：结果变量从块
入口开始可见，尚未初始化时只能作为允许的写入目标。

## 表达式

### 值类别

左值直接指代已经存在或已经物化的对象；移动左值直接指代处于移动语境中的对象；
纯右值产生新值并初始化结果对象，但表达式本身不直接指代该对象。`const` 属于表达式
类型，不构成额外值类别。`const T` 左值与移动左值都是合法的对象表达式状态；值类别
不授予修改权限。const 来源只能请求复制，不能绑定要求可修改来源的引用或执行移动操作。

| 值类别 | 含义 |
|---|---|
| 左值 | 指代已经存在或已经物化的对象 |
| 移动左值 | 指代处于移动语境中的对象 |
| 纯右值 | 产生新值并初始化结果对象，但不直接指代该对象 |

每个产生运行时对象值的表达式同时具有对象类型和值类别。物化使纯右值的结果对象成为
表达式直接指代的临时对象，从而能够被引用、访问或传递；物化后的表达式是移动左值。

尚未默认定型的精确整数表达式、`null`、函数名称和 `void` 表达式不具有对象值类别。
结构体类型名称本身不形成对象表达式；它只用于类型语法以及规定的构造或析构调用名称
位置。普通表达式位置中的名称必须形成对象表达式。

| 声明或表达式 | 表达式类型 | 值类别 |
|---|---|---|
| 具名 `T` | `T` | 左值 |
| 具名 `const T` | `const T` | 左值 |
| 具名 `mut T` | `T` | 左值 |
| 具名 `copy T` | `const T` | 左值 |
| 具名 `move T` | `T` | 左值 |
| `move value` | `T` | 移动左值 |
| `move const_value` | `const T` | 移动左值 |
| 产生新 `T` 值的表达式 | `T` | 纯右值 |

### `move` 表达式

`move value` 要求操作数是对象左值，将其转换为移动左值并保留类型及顶层 `const`。
纯右值、已经是移动左值的表达式和非对象表达式不能作为操作数。`move` 只改变表达式的
值类别，不立即移动资源；后续初始化或赋值依据实际来源的 const 权限和值类别选择操作。
因此 `move const_value` 合法，但需要形成或赋值普通对象时只能复制。

具名 `move T` 引用是 `T` 左值。需要继续把它作为移动来源传递或移出时，必须再次显式
写出 `move value`。

### 对象取地址

一元 `&object` 要求操作数是对象左值，结果是指向该实际对象的原始指针纯右值。移动
左值、纯右值和临时对象不能取地址。具名 `mut T`、`copy T` 或 `move T` 引用透明表示
referent，因此对这些表达式取地址得到 referent 的地址，不得到指向引用包装的指针。

取地址保留对象的修改权限：可修改 `T` 左值形成 `*T`，`const T` 左值和具名
`copy T` referent 形成 `*const T`。具名 `mut T` 与 `move T` referent 仍形成 `*T`。
字段左值、解引用结果、结果为左值的条件表达式和结果为左值的赋值表达式都可以取地址。例如：

```cw
&object.field
&*pointer
&(condition ? first : second)
&(first = second)
```

各子表达式仍按自身规则求值；取地址不会消除解引用、赋值或条件目标选择。取整个对象的
地址要求该对象已经整体初始化；取已初始化子对象的地址只要求该子对象已经初始化。取地址
本身不改变初始化状态。
通过指针值追踪 pointee 的初始化状态、指针逃逸与悬垂检查尚未规定。普通函数名称不是
本节的对象操作数；函数取地址使用下节的独立规则。

### 函数取地址与虚接口指针

普通函数指针类型写为 `*func (parameters) return-type`。虚接口指针类型写为
`virtual *func (parameters) return-type`；其中函数类型的第一形参必须是以结构体为
referent 的 `mut`、`copy` 或 `move` 引用，并作为虚调用的接收者。两类指针是不同的
运行时对象类型，均可存储、传递和间接调用，不互相隐式转换。

函数取地址必须显式写出 `&`：

```cw
var callback *func (i32) i32 := &Transform;
var operation *func (copy Value) copy Value := &operator -;
var slot virtual *func (copy Base, i32) i32 := &Draw;
```

当 `name` 绑定函数符号时，`&name` 表示可见的同名函数重载集合；`&operator op` 表示
对应的用户定义运算符函数集合。取址本身不根据候选数量推导类型，必须由当前直接语境提供
目标类型。直接语境包括具有显式类型的变量初始化、已知目标类型的 `:=` 或 `=`、返回对象，
以及当前正在评估的外层函数、运算符函数或构造函数候选的形参。括号只保留分组，不阻断
直接目标；目标不会继续传播进条件表达式或其它内部运算。

选择只接受与目标完整精确相同的候选类型，包括全部形参、引用模式、返回类型以及外层
普通或虚接口指针种类。普通函数指针目标只选择普通函数定义；虚接口指针目标只选择虚接口
声明，抽象虚接口也可以被选择。构造函数、析构函数和内建运算符不形成取址候选。没有目标
时即使只有一个候选也非法；目标不是上述两类类型、没有精确候选或存在多个精确候选也都
非法，不会改用普通调用的隐式转换或重载排序。

成功的函数取址产生目标指针类型的纯右值。函数名称本身既不是对象，也不会隐式退化为
函数指针；它只能被直接调用或在上述显式取址形式中使用。局部对象若遮蔽同名函数，`&name`
仍按对象取地址规则处理，不会绕过遮蔽重新查找函数。`&operator op` 只表示运算符函数取址；
它与 `operator op(arguments)` 形式的显式调用相互独立，仍采用上述目标驱动的精确选择规则。

`F;`、`(F);` 等独立表达式语句若仍表示函数重载集合，则是错误；只有一个候选也一样。
括号保留内部表达式的状态，不使函数名称成为可独立求值的对象。

### 指针解引用

一元 `*pointer` 要求操作数是指向对象类型的原始指针，并形成其直接 pointee 的左值；
pointee 的 `const` 限定完整保留。解引用会读取指针对象本身，因此该指针必须已经初始化。
`*void` 等不指向对象类型的指针不能解引用为对象表达式。

### 数组值与下标

完整数组值写为：

```cw
[3] i32 { 1, 2, 3 }
[2] [3] i32 { { 1, 2, 3 }, { 4, 5, 6 } }
[0] i32 {}
```

最外层必须在花括号前写出完整的 `[N] T` 类型。嵌套花括号列表只能在外层数组
已提供对应元素类型时出现；脱离这种语境的 `{ ... }` 不是独立数组值。一层
列表只形成当前一层元素，不用扁平列表跨越内层数组边界。

初始化项数量必须与 `N` 精确相等；当前没有长度推导、缺失元素补齐或重复初始化。
第 `i` 项按普通对象形成规则直接形成元素 `i`；各项从左到右求值，只有所有
元素均成功形成时，表达式才产生完整的 `[N] T` 纯右值。零长度列表本来就
含零个元素，因此 `[0] T {}` 合法。

后缀下标 `array[index]` 当前只接受数组基础表达式，`index` 必须精确具有 `usize`
类型；固定宽度整数和默认定型的整数字面量不因出现在下标中获得例外。数组
左值的下标结果是元素左值，数组移动左值的结果是元素移动左值；数组纯右值先
物化为临时数组，其下标结果是该临时对象中的元素移动左值。数组本层或元素
类型的 `const` 都传播到结果元素，不改变上述值类别；结果可以是 const 移动左值。

原始指针下标及所有下标越界政策尚未规定；数组不因提供下标表达式而隐式退化为
元素指针。

### 字符串字面量

字符串字面量表示一个已经初始化、具有静态存储期的只读定长字节数组。表达式具有
`const [N] u8` 类型和左值类别；`const` 限定整个数组对象。

`N` 是转义解码及相邻字面量拼接后的内容字节数，不包含隐式终止零。显式写出的
`\0` 属于数组内容并计入长度。未转义内容保留源码字节，不转码，也不要求内容为有效
UTF-8；长度不按 Unicode 字符数量计算。

| 字面量 | 类型 |
|---|---|
| `"abc"` | `const [3] u8` |
| `""` | `const [0] u8` |
| `"a\0b"` | `const [3] u8` |
| `"abc\0"` | `const [4] u8` |
| `"\xFF"` | `const [1] u8` |
| `"a" "b"` | `const [2] u8` |

字符串字面量遵守普通数组的初始化、赋值、传参、返回及引用绑定规则。普通值推导
移除数组本层的 `const`，复制形成独立的可修改数组；局部与全局声明适用相同推导规则。
显式取地址得到指向原始只读数组的指针，下标结果是 `const u8` 左值：

```cw
func Example(index usize) {
  var text := "abc";       // 独立的 [3] u8 对象
  text[index] = 'x';
  var pointer := &"abc";   // *const [3] u8
  (*pointer)[index];       // const u8 左值
}
```

数组长度是类型身份的一部分，不因目标类型而改变。字符串字面量不隐式退化为元素
指针，也不因物理存储中存在额外字节而扩展数组的内容或下标范围。

### 字面量定型

- 布尔字面量具有 `bool` 类型。
- 字符字面量具有 `u8` 类型。
- 字符串字面量具有 `const [N] u8` 类型，其中 `N` 为内容字节数。
- 带 `f` 或 `F` 后缀的浮点字面量具有 `f32` 类型，其他浮点字面量具有 `f64` 类型。
- 尚未定型的精确整数表达式在需要运行时整数值或到达完整表达式边界时，按既定顺序
  默认定型为 `i32`、`i64` 或 `u64`。

变量初始化、函数实参、返回表达式、表达式语句和其他运算符的操作数都是需要完成默认
定型的使用位置。字面量的目标使用位置不会反向改变其初始定型结果。

### 一元数值与逻辑运算

对尚未默认定型的精确整数表达式使用一元 `+` 或 `-` 时执行精确计算，结果仍保持精确
且尚未定型。

运行时一元 `+` 接受固定宽度整数、尺寸整数和浮点数，并保留操作数类型。运行时一元
`-` 接受有符号固定宽度整数、`isize` 和浮点数，并保留操作数类型；无符号整数不接受
普通一元负号。CW 不对小整数执行 C++ 式整数提升。

一元 `!` 只接受 `bool`，结果也是 `bool`。

### 数值算术

`+`、`-`、`*` 和 `/` 接受合法的数值类型组合；`%` 只接受整数。二元运算先确定两个
操作数的公共类型，将需要转换的一侧转换到该类型，结果也具有该公共类型。

固定宽度整数不执行小整数提升，其公共类型按以下顺序确定：

1. 类型相同时保持该类型。
2. 有符号性相同时选择位宽更大的类型。
3. 有符号与无符号混合时：若无符号类型等级不低于有符号类型，选择无符号类型。
4. 否则，若较宽的有符号类型能表示无符号类型全部值，选择该有符号类型。
5. 否则，选择较宽有符号类型对应的无符号类型。

例如：

```text
i8 + i8   -> i8
u8 + i16  -> i16
i32 + u32 -> u32
i64 + u64 -> u64
```

字符按 `u8` 参与上述规则。`f32` 与 `f64` 混合使用 `f64`；固定宽度整数与浮点数混合
使用浮点操作数的类型。

尺寸整数只在自身家族内进行内建算术：同类型保持不变，`isize` 与 `usize` 混合使用
`usize`。尺寸整数不能与固定宽度整数、字符或浮点数隐式混合。

### 比较与逻辑运算

`<`、`<=`、`>` 和 `>=` 接受能够按数值公共类型规则统一的两个操作数，结果为
`bool`。`==` 和 `!=` 还接受两个 `bool` 操作数。布尔值不支持次序比较。

原始指针的 `==`、`!=` 比较指针值，结果为 `bool`。先确定共同指针类型，再将双方合法
转换为该类型的值；左右交换不改变兼容性或共同类型：

| 操作数类型或表达式 | 共同指针类型 |
|---|---|
| `*T` 与 `*T` | `*T` |
| `*T` 与 `null` | `*T` |
| `*T` 与 `*const T` | `*const T` |
| `**T` 与 `**const T` | `*const *const T` |
| `*Derived` 与 `*Base` | `*Base`，要求唯一静态继承路径 |
| `*const Derived` 与 `*Base` | `*const Base`，要求唯一静态继承路径 |

共同类型合并各层已有的 `const`，并补齐深层限定变化所需的中间只读限定。继承统一只
处理直接结构体 pointee，使用两侧之一的基类类型；不搜索任意共同祖先，不递归转换
内部指针的继承关系，也不增加到 `*void` 的转换。因此 `*i32` 与 `*f64`、`**Derived`
与 `**Base`、两个不同派生类的指针均不能据此比较。指针不支持次序比较。

普通函数指针和虚接口指针只支持与 `null` 的 `==`、`!=`，左右对称。两个已定型的函数
指针或虚接口指针暂不允许相互比较，即使类型相同或都保存空值；与 `&F` 的比较也不允许。
`&F` 与 `null` 都不能提供具体指针目标，因此不能自行形成比较。括号不改变上述规则。

`null == null` 为 `true`，`null != null` 为 `false`，两者都不需要先转换为某个指针类型。
指针与 `null` 比较用于判空，不比较所指对象的内容。

`&&` 和 `||` 的两个操作数都必须是 `bool`，结果为 `bool`。CW 不提供从整数、浮点
数或指针到 `bool` 的隐式真假转换。

### 条件表达式

条件表达式写为：

```cw
condition ? then_expression : else_expression
```

条件必须为 `bool`。两个分支必须都是 `void`，或者都能形成兼容的运行时对象类型；
函数类型等其他非对象类型不能形成条件表达式的值结果；下述指针与 `null` 组合可以形成
指针对象结果。不同数值对象类型使用数值公共
类型规则。`bool` 与数值类型不兼容，`void` 与非 `void` 类型不兼容。

分支先独立定型，再确定公共类型；外围变量、返回类型或函数形参不会反向影响公共类型。
两个 `void` 分支形成 `void` 结果且没有对象值类别。两个分支去除顶层 `const` 后具有
相同对象类型且同为左值时，结果保留左值类别；同为移动左值时，结果保留移动左值类别。
这两种情况下，任一分支带顶层 `const` 都会使结果带顶层 `const`；两个分支保持原有
类型和值类别，不发生转换或物化。例如 `flag ? move a : move b` 中若 `b` 为 const，
结果是 const 移动左值，即使运行时选中非 const 的 `a`，也只能按 const 来源使用。

其它平凡对象或数值对象的兼容分支组合形成新的公共类型结果对象，条件表达式为纯右值。
相同底层对象类型的值结果移除顶层 `const`；不同数值类型按公共数值类型转换。对这些
平凡结果，左值、移动左值与纯右值任意混合时均采用值结果规则；若外围引用绑定需要直接
指代结果对象，物化整个条件表达式，而不是分别物化两个分支。

对于去除顶层 `const` 后同型的非平凡对象分支，两个左值仍保留左值结果，两个移动左值
仍保留移动左值结果，两个纯右值仍直接形成纯右值结果。其它混合组合形成纯右值结果：
每个左值分支及 const 移动左值分支分别使用复制形成，非 const 移动左值分支优先使用
移动形成并允许退回复制形成，纯右值分支保持不变。两个分支都必须能够形成共同结果，
任一分支失败时不部分提交另一分支的构造。运行时仍只求值被选择的分支。
此处的非平凡对象包括非平凡结构体和非平凡数组；数组分支必须是完全相同的数组类型。

一个分支为已定型的原始指针、普通函数指针或虚接口指针，另一个为 `null` 时，两侧均
形成该指针类型的值，条件表达式为纯右值，移除结果类型的顶层 `const`。不同具体原始
指针分支复用上述共同指针类型规则。同型左值或移动左值分支仍优先适用保留对象身份的
规则；同型函数指针分支也可选择，这不属于函数指针相等比较。

两个 `null` 分支不能形成对象结果，`&F` 与 `null` 分支也没有具体指针目标；即使外围
提供显式指针类型，这两种组合仍然错误。运行时仅求值被选择的分支。

### 隐式转换

类型相同不产生转换。CW 允许以下内建隐式转换：

- 任意两个固定宽度整数类型之间；
- 固定宽度整数与 `f32`、`f64` 之间；
- `f32` 与 `f64` 之间；
- `isize` 与 `usize` 之间；
- 相同的布尔、指针、结构体或数组类型之间；
- 原始指针按照“指针类型”一节进行的安全限定转换；
- `null` 直接形成合法的原始指针、普通函数指针或虚接口指针空值；
- 原始指针直接 pointee 沿唯一静态继承路径进行的派生到基类转换，以及该转换与直接
  pointee 限定增加的组合。

普通按值读取和转换忽略源表达式当前对象层的顶层 `const`，但保留所有嵌套限定。因而
`const T` 可以恒等复制初始化 `T` 或参与 `T` 的只读内建运算；移除该层限定不构成
额外的隐式转换，也不允许修改或移动源对象。

尺寸整数与固定宽度整数或浮点数之间没有隐式转换。`bool` 与数值类型之间没有隐式
转换。不同结构体或数组类型之间没有内建隐式转换；数组不执行逐元素转换，也不退化为
元素指针。不同函数签名的函数指针之间没有内建隐式转换。除上述安全限定转换和直接
结构体 pointee 的派生到基类转换外，其他不同原始指针类型之间没有内建隐式转换。

允许但可能丢失信息的转换仍然成功，同时产生警告：

- 整数目标位宽小于源位宽时，警告可能截断；同位宽有符号性变化或扩大位宽不属于
  截断警告。
- 整数转换为浮点数时，只有目标浮点类型不能精确表示源整数类型的全部值才警告。
  `f32` 提供 24 位二进制有效精度，`f64` 提供 53 位。
- `f64` 转换为 `f32` 时警告可能损失精度或范围；`f32` 转换为 `f64` 不警告。
- 浮点数转换为固定宽度整数时，警告可能丢失小数部分或超出目标范围。

上述判断只取决于源、目标类型，不因当前表达式恰好可精确表示而取消警告。

### 运算符优先级与结合性

下表从高到低列出本文定义的表达式层级：

| 层级 | 运算符 | 结合性 |
|---|---|---|
| 后缀 | 调用 `()`、下标 `[]`、`.`、`->`、后缀 `++`、`--` | 从左到右 |
| 一元 | `move`、`&`、`*`、`+`、`-`、`!` | 从右到左 |
| 乘法 | `*`、`/`、`%` | 从左到右 |
| 加法 | `+`、`-` | 从左到右 |
| 次序比较 | `<`、`<=`、`>`、`>=` | 从左到右 |
| 相等比较 | `==`、`!=` | 从左到右 |
| 逻辑与 | `&&` | 从左到右 |
| 逻辑或 | `||` | 从左到右 |
| 条件 | `?:` | 从右到左 |
| 初始化与赋值 | `:=`、`=` | 从右到左 |

调用、下标、字段访问和后缀自增减属于同一个后缀层，并从左到右连续组合。语法允许在
任意后缀表达式后继续写调用实参；该形式是否具有可调用类型，以及应执行直接调用、构造、
间接调用、结构体可调用对象调用还是接收者调用，仍由后续语义规则决定。

括号可以显式改变分组，并作为源码分组边界保留。对于已经独立成立的对象表达式，括号
不改变类型、值类别或对象身份；函数指针对象等可调用对象因此可以写成 `(callback)(args)`。
普通函数名和类型名本身不是对象表达式，只有直接形式 `F(args)` 和 `T(args)` 分别取得
具名函数调用和构造含义；当 `F` 或 `T` 只表示普通函数或类型时，`(F)(args)` 和
`(T)(args)` 非法。字段访问后是否立即追加调用括号还用于区分接收者调用与字段值调用，
详见“接收者调用”。

## 初始化与赋值

### 变量声明

变量组由 `var` 引导。每个名称后的显式类型只属于该变量，不向同组其它变量传播：

```cw
var value i32;
var value := expression;
var value i64 := expression;
var first i32, second bool := expression1, expression2;
var inferred, explicit i64 := 1, 2;
var value i32 := {
  // 变量初始化块
}
```

全局与局部直接初始化均允许逐变量省略类型，并从位置对应的唯一来源推导；同一组
可以混写显式类型和推导。`var a, b i64 := 1, 2;` 中，`a` 推导为 `i32`，只有 `b`
显式为 `i64`。显式类型含错时不会改为推导。

变量初始化块必须为每个结果分别写出类型，结果可以异型，不从块体控制流反推类型。
局部变量可以仅声明而不初始化，但每个变量都必须显式写出类型。全局变量必须在声明处
提供直接初始化来源或初始化块；平凡、非平凡、空及非空对象均遵守这一要求。

直接初始化表达式列表必须与变量列表等长并按位置对应；各项从左到右求值，完成对应
变量的初始化后才处理下一项。各项初始化不改变该声明组既有的名称可见性规则。省略
显式类型时，同一组中的不同变量可以从各自来源得到不同类型。编译期整数先物化为默认
运行时整数，再用于推导；外围声明不会反向决定字面量类型。

推导变量始终建立新的普通拥有对象，不隐式推导引用。初始化表达式透明呈现引用的
referent 类型；推导时移除该对象当前层的顶层 `const`，保留指针 pointee 等所有嵌套
限定。例如 `const *T` 来源推导为 `*T`，`*const T` 来源仍推导为 `*const T`。
平凡对象的左值、移动左值和纯右值来源随后分别按复制初始化、移动初始化或直接初始化
规则处理。非平凡对象的同型左值、移动左值和纯右值分别按复制形成、移动形成或直接
传递规则形成推导出的对象。不能形成运行时对象类型的表达式不能用于省略类型的变量推导。
推导出的对象仍须满足相应语境的类型资格，例如不能借助推导形成抽象类的完整值对象。

### 全局初始化

当前翻译单元中的全局初始化构成统一执行体，变量组按声明顺序初始化。初始化表达式
可以调用普通运行时函数，不要求编译期求值，也不按函数体依赖重新排序：

```cw
var seed := MakeSeed();
var count i32, ready bool := {
  var helper := seed;
  helper, true
}
func MakeSeed() i32 { 1 }
```

直接初始化列表中的本组名称在全部来源分析后才可见，因此不能直接引用自身或同组变量。
初始化块的结果名称从入口可见，但在形成前不能读取；可以通过 `:=` 或尾表达式形成。
全局变量属于统一外层作用域，块内局部变量属于嵌套作用域。内层局部对象可以先形成，
再用来形成外层结果；退出块时外层结果继续存活。全局初始化完成不结束全局对象生命周期。

全局执行体复用确定初始化、对象形成顺序及块结果完成检查。普通函数、构造函数和析构
函数的函数体独立分析，可以访问全部全局变量，但不能通过 `:=` 补做全局对象初始化。
当前检查不跨越普通调用、函数指针或虚调用传播全局状态，经调用间接提前访问全局对象
尚未被检查覆盖，也不因此保证安全或取得零值。是否增加运行时状态追踪留待后续决定。

当前前端保留语义化的全局声明及其初始化子树，不生成隐藏的全局函数或清理列表。实际
初始化／析构入口、跨翻译单元顺序及平台启动和卸载接入留给后续 Generator 工作。

### 确定初始化

平凡结构体对象按其平凡基类和字段递归分解。内建对象、原始指针、函数指针和虚接口指针是叶子；
非平凡结构体字段也是不可继续分解的叶子，必须作为整体构造。一个平凡聚合子对象在其
覆盖的全部叶子都初始化完毕时完整。没有基类和字段的平凡空对象自身是一个状态叶子，
必须由实际初始化形成；普通对象不会仅因大小为零或没有成员就在声明处完整。
数组不论平凡性和长度，在确定初始化中始终作为一个整体叶子，不按下标展开状态。

普通非平凡对象始终作为一个整体叶子，即使它没有字段和基类，也必须通过构造函数出生。
构造函数中的 `this` 单独按构造义务分解：直接基类是一个整体叶子，平凡字段递归分解，
非平凡字段是整体叶子。没有基类和字段时，构造函数入口处的 `this` 已经完整。

对尚未整体初始化完毕的对象，不得把它当作完整对象使用，包括取整个对象的地址、绑定到
`mut`/`copy`/`move` 引用、按值传递，或以它为接收者。已经初始化的子对象是普通左值，
可以读取、取其地址、绑定引用或作为接收者。`move` 采用 moved-from 模型，不把来源标回
未初始化，也不改变确定初始化状态。

平凡结构体可以先声明、再对其尚未初始化的平凡子对象逐项使用 `:=`。非平凡结构体必须
通过构造函数出生，不得在构造函数之外靠初始化后代字段拼出完整对象。非平凡字段也不能
通过 `field.member := ...` 绕过其整体构造；应以该字段整体作为初始化目标。
平凡派生对象的具名基类子对象同样可以整体作为目标，例如 `object.Base := source`；成功时
一次性初始化该基类范围的全部叶子。路径可以继续穿过平凡中间子对象，但不能越过非平凡
原子子对象直接初始化其内部字段或基类。
数组只能由完整同型来源或完整数组值一次完成初始化。`array[index] := value` 及更深的
元素投影不能用来逐项推进数组的初始化状态。完整数组已初始化后，元素读取或赋值
按普通子对象规则处理，但不建立独立的逐元素确定初始化状态。零长度数组也不例外。

初始化表达式 `target := expression` 只初始化尚未初始化的目标；若目标已经初始化，则
诊断重复初始化。简单赋值 `target = expression` 只修改已经初始化的可修改左值；其左端
按普通对象使用规则检查，尚未初始化时诊断使用未初始化对象。两个运算符的含义固定，
不会根据控制流状态互相转换。

`:=` 必须直接作为表达式语句的根出现，具有 `void` 类型且不产生对象值。它的目标必须是
尚未初始化的直接具名对象（局部变量、变量初始化块结果或函数返回对象），或从可分解的
初始化根通过点号选择的字段或基类子对象；目标的根和整条投影路径都不得包含分组括号。
构造函数中的当前 `this` 还可以按照下文规则作为委托目标，或作为字段、直接基类子对象的
初始化根。因此 `(target := source);`、`(target) := source;`、
`(object).field := source;` 和 `(object.field) := source;` 均非法；
`target := (source);` 合法，初始化来源中的括号按普通表达式规则处理。普通赋值没有这项
源码目标限制，`(target) = source` 仍按普通 place 表达式规则处理。

声明中的 `:=` 只分隔声明与初始化来源，不属于初始化表达式本身。`=` 是普通表达式。
内建简单赋值的结果具有目标的类型、对象身份和值类别；用户 `operator=` 的结果按该函数
返回类型形成，不保证结果身份是左端。因此 `=` 仍可右结合形成链式赋值，但链式能否成立
取决于返回类型。

`target := source` 与 `target = source` 都先完整求值 `source`，再求值左端，最后完成
初始化或赋值。`:=` 的直接具名目标位置不读取该对象；字段基础、下标或其他复合目标中
需要求值的子表达式仍在来源之后求值。`=` 的左端始终是普通对象使用，求值时要求相应
对象已经初始化。源码写为 `lhs = rhs` 时，该先右后左的顺序不因选中用户 `operator=`
而改成普通函数实参顺序。

若控制流合流时同一对象在一部分可达路径上已经初始化、另一部分路径上尚未初始化，程序
错误。合流分别比较对象的每个叶子子对象；已经终止的路径不参与之后的合流。循环
不得承担把对象从部分初始化推进到完整初始化的职责。

对象的生命周期从初始化成功时开始。正常退出时的清理责任、顺序及完整表达式边界，
见后文“对象生命周期”。

`const T` 对象完成初始化后不得再次赋值；尚未初始化的 `const T` 对象仍然可以通过
`:=` 完成初始化。

使用 `:=` 初始化引用对象时建立引用绑定。引用一旦完成绑定便不能重新绑定；具名引用
表达式透明地表示 referent，因此对具名 `mut T` 或 `move T` 引用的普通赋值修改
referent，对具名 `copy T` 引用的赋值非法。建立引用绑定与修改 referent 是不同的
语义操作。引用只能绑定到已经完整的 referent，因此不能通过引用表达式对 referent 的
字段使用 `:=`；字段写入应使用普通赋值 `=`。

### 复制、移动与简单赋值

平凡结构体可以从同型左值、移动左值或纯右值初始化：从左值或移动左值初始化时按位复制
已经开始生命周期的对象表示；从纯右值初始化时，来源结果直接成为目标。上述过程不调用
构造函数。
平凡数组沿用同一普通对象路径；语言级效果是按下标递增复制各元素，实现可在不改变
可观察结果时使用整块复制。数组纯右值同样直接形成目标。

非平凡结构体可以显式声明复制构造和移动构造：

```cw
ctor T(other copy T) { /* ... */ }
ctor T(other move T) { /* ... */ }
```

目标类型为 `T`、接口有效且恰有一个精确 `copy T` 形参的构造函数是复制构造；相同条件下
具有精确 `move T` 形参的构造函数是移动构造。两者仍属于 `T` 的普通构造重载集，显式
`T(arguments)` 继续在全部构造函数中执行普通重载决议。语言不会隐式生成复制构造或
移动构造，也不会从其它构造函数形态推断这种身份。

从同型来源形成新的非平凡按值 `T` 对象（结构体或数组）时，采用以下规则：

| 来源 | 对象形成 |
|---|---|
| `T` 左值 | 复制构造 |
| `const T` 左值 | 复制构造 |
| `T` 移动左值 | 优先移动构造；没有移动构造时退回复制构造 |
| `const T` 移动左值 | 复制构造 |
| `T` 纯右值 | 直接形成目标，不再调用构造函数 |

const 来源缺少复制能力时，对象形成失败，即使存在移动构造也不能改为移动；非 const
移动来源缺少移动与复制能力时同样失败。纯右值直接传递不执行复制
或移动，也不插入额外转换或临时物化。该纯右值可以来自返回 `T` 的普通函数、
同型条件表达式或其它表达式；当 `T` 是结构体时也可来自 `T(arguments)`。规则不因
纯右值的来源不同而改变。

数组自身不声明或合成构造函数。对非平凡数组请求复制或移动形成时，语义递归到
最终非平凡元素的构造能力，并按下标递增形成元素。来源元素继承各层数组的 const：
const 元素只复制，非 const 移动元素优先移动、允许复制回退。嵌套数组递归应用同一规则。
零长度数组仍按元素类型检查这些能力，
但运行时不形成任何元素。

这套对象形成规则统一用于变量与字段初始化、直接声明初始化、初始化块结果、具名或不具名
返回对象，以及普通函数、构造函数、函数指针和运算符函数的按值实参。引用绑定不创建新
对象，继续使用引用规则。

`var a T := T(other)` 先按显式实参选择 `T` 的构造函数，再让产生的纯右值直接形成 `a`；
`var a T := Make()` 在 `Make` 返回 `T` 时采用相同规则，外层初始化都不会再次调用构造
函数。`var a T := other` 根据 `other` 的 const 权限和值类别选择复制构造或移动构造。
当 `U` 派生自 `T` 时，`var a T := u` 先选择 `u` 中的 `T` 基类子对象，再按同型规则
读取、复制或移动，形成独立的 `T` 对象。这种按值切片适用于全部普通对象初始化位置，
包括按值形参、返回对象、字段、基类子对象和数组元素。`U` 纯右值先物化完整的 `U`，
再以其中 `T` 的移动左值执行上述规则。基类子对象继承来源的 const；const 基类移动
左值仍只读取或复制。非 const 对象含有 const 成员不禁止整体移动，用户构造函数体内
对各成员的操作分别服从成员的 const 权限；语言不会因此隐式生成逐成员构造函数。
其它 `U ≠ T` 的情况不因存在转换构造而成为隐式转换；要转换须显式写 `T(u)`。普通赋值
不因初始化支持切片而获得派生对象到基类对象的隐式赋值能力。

在重载决议中，同型非平凡对象到按值 `T` 形参先按精确值序列参与候选匹配；此时不查询
复制构造、移动构造或复制回退是否可用。派生到基类按值匹配也先按基类投影关系参与比较，
选中后才检查投影所得同型对象的形成能力。同一实参对按值 `T` 形参和同一目标对象类型的
直接引用形参同时可行时，两条序列在该实参位置不可区分；不会仅因按值路径稍后需要复制或移动构造
就自动偏向引用。其它实参位置仍可决定唯一最佳候选；若所有位置都不能区分，则调用歧义。

选出唯一最佳候选后，调用才按实参 const 权限和值类别为每个非引用按值形参形成实际对象。
任何形参对象形成失败都使这个已经选定的调用出错，不会删除该候选、改选次优重载或改报
没有匹配函数。
只有全部形参的转换都补全成功后，才按实参顺序报告转换风险并统一提交实参转换。
形成失败仍归属于已经选定的调用，但调用本身不产生结果值；所有实参保留来源准备后的
形态，不为未采用的转换报告风险。

同型简单赋值按来源的 const 权限和值类别选择：

| 来源 | 赋值 |
|---|---|
| `T` 左值 | 复制赋值 |
| `T` 移动左值 | 移动赋值；没有则复制赋值 |
| `T` 纯右值 | 先物化，再按移动左值赋值 |
| `const T` 左值 | 复制赋值 |
| `const T` 移动左值 | 复制赋值 |

平凡结构体的同型 `=` 按位复制，结果是左端左值，并保有目标
对象身份。非平凡结构体的同型 `=` 只通过复制赋值或移动赋值进行；两者都没有则错误。
平凡数组沿用普通内建 `=`；非平凡数组递归选择最终元素的复制或移动赋值，并按下标
递增赋值各元素。实际来源元素继承各层数组的 const；const 来源只选择复制赋值，缺少时
报错；非 const 移动来源优先移动赋值、允许退回复制赋值。零长度仍检查元素赋值能力。
数组赋值的目标必须整体已初始化、可修改且不含 `const` 元素；结果固定为左端数组左值并
保留其对象身份。
用户 `operator=` 的表达式结果按返回类型形成。左右端去除顶层 `const` 后类型不同的
`=` 如何选择其它 `operator=` 重载另行规定。

### 变量初始化块

变量初始化块允许通过多条语句和结构化控制流完成一组变量的初始化：

```cw
var first i32, second i32 := {
  first := 1;
  second := first + 1;
}
```

变量初始化块必须为每个结果变量分别显式写出类型，结果可以不同类型，并使用 `:=` 引出
花括号。声明组中的结果变量从块入口开始可见。有初始化叶子的结果从块入口起尚未初始化；
平凡空结构体结果同样具有状态叶子，必须实际初始化。初始化完成前，不得把结果
变量当作完整对象使用；若结果是平凡结构体，允许对其尚未初始化的字段使用 `:=`，并允许
使用已经初始化的字段。可以将尚未初始化的结果作为 `:=` 的直接目标；对已经完整的平凡
空结果写 `:=` 属于重复初始化。

块末尾没有分号的表达式列表是尾表达式。尾表达式与完整结果变量列表等长，并按固定
位置对应；各表达式从左到右求值，每项求值后立即写入对应结果，因此后项可以读取已由
前项初始化的结果变量。

每个尾表达式固定执行对应结果对象的初始化。若该结果此前已经初始化，则诊断重复初始化；
尾表达式不会改为赋值，也不会覆盖已有结果。

初始化块末尾的最后一个 `if` 可以由各分支继续完成同一组结果变量。每条能够正常完成
块的路径都必须使全部结果变量已经初始化；缺少 `else` 的假分支保留进入 `if` 前的
状态。循环不能承担这项结果初始化职责。

变量初始化块中不得使用 `return`。

## 函数

### 普通函数

普通函数声明写为：

```cw
func name(parameter_list) return_declaration {
  // 函数返回块
}
```

具名形参写为 `name Type`。返回声明可以省略、直接写类型，或使用具名返回对象：

```cw
func observe(value i32) void {}
func compute(value i32) i64 {}
func compute(value i32) var result i64 {}
func action() {}
```

函数声明中的每个形参都必须写出名称；形参类型不能直接出现在参数名称位置。普通具名
函数、运算符函数和虚函数声明的第一形参还可以使用保留名称 `this`，但该形参必须具有
`mut T`、`copy T` 或 `move T` 引用类型：

```cw
func Read(this copy T) void {}
func Write(this mut T, value i32) void {}
func Consume(this move T) void {}
```

这里的 `this` 是进入函数类型和调用实参序列的真实第一形参。它在函数体中按照普通
形参名称绑定；相应的引用模式决定其类型、权限和值类别。`this` 不得用作后续形参名称，
也不得命名普通按值形参或原始指针形参。引用一个指针对象仍然合法，例如
`this copy *T`；这不同于非法的指针形参 `this *T`。

构造函数和析构函数不得声明显式 `this` 形参。它们的函数体仍使用不进入函数类型的
隐式目标 `this`。普通函数没有显式声明名为 `this` 的第一形参时，函数体中的 `this`
不隐式指代其它名称的首形参。

省略返回声明的普通函数返回 `void`。具名与匿名的非 `void` 返回声明都建立唯一返回
对象；名称只决定函数体能否直接引用该对象，不改变返回规则。

普通对象形参在每次调用中形成独立对象，由对应实参初始化。其生命周期在初始化成功后
开始。形参对象的正常析构按“对象生命周期”中的 ABI 配置规则处理。对于实参表达式直接
指代的已有对象，按值传参不结束该对象的生命周期。

非平凡对象的普通按值形参使用前述统一对象形成规则。同型实参先以精确值序列参与外层
候选选择；选出唯一函数后，同型纯右值直接形成形参对象，同型左值使用复制形成，同型移动
左值优先使用移动形成并允许退回复制形成。选后形成失败不回退重载。这一规则同样适用于
普通函数、构造函数、运算符函数和函数指针调用；引用形参使用下述统一引用绑定规则。
若形参类型是数组，选后形成递归使用元素类型的最终复制或移动能力，不使数组事先退化为指针。

引用形参只建立引用绑定。被调函数退出时该绑定结束；referent 仍按自身的生命周期
规则处理。

普通按值形参和普通按值返回不得带顶层 `const`。调用方需要只读保存返回对象时，应在
自己的目标对象上使用 `const T`；需要返回已有对象的只读访问时，应使用 `copy T`。
指针 pointee 等嵌套位置的 `const` 以及三种引用模式继续属于函数接口类型。

### 重载声明与决议

普通直接调用写为 `name(arguments)`。只有调用括号直接左侧的简单名称参与普通函数名称
查找；分组后的 `(name)` 先按普通对象表达式独立分析，不穿过括号恢复具名函数调用身份。

同名普通函数可以重载。重载签名由按源码顺序排列的形参类型组成；返回类型和形参名称
不参与区分。相同签名的再次声明是错误，即使返回类型不同。引用模式和合法的嵌套
`const` 都参与形参类型身份；普通按值形参的顶层 `const` 本身非法，不能用于建立重载。

对只涉及普通值形参和内建隐式转换的调用，实参数量必须与候选形参数量相同，每个实参
都必须能够执行允许的隐式转换。数值类型的可行转换按以下等级比较：

1. 精确匹配：源、目标类型相同。
2. 保值转换：目标类型能够精确表示源类型的全部语言级值。
3. 可能丢失信息的转换：转换合法，但不能保证保持全部值。

原始指针转换采用以下比较规则：完整同型匹配优于仅增加限定的匹配，仅增加限定的匹配优于
需要派生到基类地址调整的匹配。都需要地址调整时，目标越接近源类型越好；目标基类相同时，
限定更少的目标更好。因此对于 `Derived : Middle : Base` 和来源 `*Derived`，匹配顺序依次为
`*Derived`、`*const Derived`、`*Middle`、`*const Middle`、`*Base`、`*const Base`。
多级限定目标之间，若一个目标可以继续执行安全限定转换成为另一个目标而反向不成立，前者
更好。不可行的限定移除和缺少中间只读屏障的转换不参与候选比较。

`null` 按每个候选的目标直接形成具体指针值。不同具体指针目标之间不可区分，不根据
指针类别、限定多少或继承关系偏向任何目标。目标对象类型相同时，`move P` 优于
`copy P`，按值 `P` 与可行的同型引用匹配不可区分；其它实参仍可决定唯一最佳候选。
已经定型的指针对象即使保存空值，也按普通指针转换比较。

数值比较按完整值域分类，不因当前值恰好可表示而改变等级，也不按位宽差细分同一等级。
例如 `i16` 面对 `i32` 与 `i64` 形参时，两者同为保值转换。

每个实参上的两条转换只产生更好、更差或不可区分三种关系。引用与按值匹配共同应用数值、
基类及限定比较；读取、物化、只读投影或对象形成的操作数量不构成优先级。
候选 A 仅当它在每个实参上都更好或不可区分，并且至少一项更优时，才严格优于 B。
调用必须存在唯一一个严格优于全部其他可行候选的函数；否则调用歧义。声明顺序和调用
所在位置期望的结果类型都不能打破平局，候选返回类型不参与决议。
单个实参不可区分不会阻断其它实参决胜；不可区分也不是可传递的等价关系。例如同型
按值 `T` 分别与 `mut T`、`copy T` 不可区分，而 `T` 左值对后两者优先匹配 `mut T`。
三个候选同时存在且没有其它实参决胜时，调用仍然歧义。歧义诊断按声明顺序展示全部
可行候选，包括被其它候选击败但仍可行的声明；没有可行候选时报告无匹配及候选失败原因。

对同一个 `&F`，不同目标函数指针类型可以各自精确选择不同声明。不同所选指针类型之间
不可区分，所选声明身份不提供优先级；其它实参仍可决定胜者。引用模式比较仅在目标对象
类型相同时适用，因而不同指针类型的 `move P` 与 `copy Q` 不因引用模式而分出优劣。

整数字面量、浮点字面量、字符和布尔实参先按自身固定规则定型，再参与重载决议；候选
形参类型不反向改变字面量类型。

选中函数后，先为全部形参检查普通对象转换、引用绑定和必要的非平凡对象形成；全部成功后
才统一生效。选后对象形成失败不尝试其它候选，且调用表达式不产生结果。成功调用的类型和
值类别由“调用结果与引用返回”规定。

### 函数指针与虚接口指针调用

类型为 `*func (parameters) return-type` 的函数指针对象可以作为调用目标：

```cw
func Invoke(callback *func (i16) i32, value i8) i32 {
  callback(value)
}
```

虚接口指针对象使用相同的调用表达式，其中第一个实参是接收者：

```cw
func InvokeVirtual(slot virtual *func (copy Base, i32) i32,
                   object copy Base, value i32) i32 {
  slot(object, value)
}
```

普通函数指针调用直接调用保存的函数；虚接口指针调用使用保存的虚槽身份和第一个实参所指
对象的动态类型执行虚派发。两者都使用内部函数类型中的唯一签名，不进行名称重载决议。
实参数量必须与形参数量相同；普通按值实参使用与普通直接调用相同的匹配及随后对象形成
规则，引用实参使用相同的引用绑定规则。任一形参形成失败时，调用目标读取和其它实参
适配都不生效。
调用目标本身按普通对象读取，尚未初始化的函数指针或虚接口指针不能用于调用。

函数指针字段也可以在字段访问外显式写出调用括号，例如
`(object.callback)(arguments)`。该形式调用字段中的函数指针值；
`object.callback(arguments)` 仍是接收者调用，不在两种形式之间回退。

调用结果若仍是函数指针，可以继续追加调用括号，例如 `factory()(arguments)`。这里内层
调用先按自身函数类型形成函数指针结果，外层调用再使用该结果；连续后缀调用在语法上
成立，并不为原本不可调用的结果增加调用能力。

两种间接调用的结果只由内部函数类型的返回类型决定，与“调用结果与引用返回”中的普通
直接调用一致。普通函数名称不会在本规则下自动转换为函数指针。

### 结构体可调用对象

顶层普通 `operator()` 函数为结构体对象定义调用协议：

```cw
trivial struct Callable {}

func operator()(object mut Callable, value i32) i32 {
  value
}

func Invoke(object mut Callable, value i32) i32 {
  object(value)
}
```

有效的 `operator()` 至少具有一个形参，且第一形参必须是 `mut T`、`copy T` 或
`move T`，其中 `T` 是结构体。按值结构体、内建类型和原始指针都不能作为该首形参。
第一形参是进入函数类型、重载身份、函数取址目标和调用实参序列的真实形参；它的名称
不决定接收者资格。

对 `object(arguments)`，调用目标必须先独立形成具有有效值类别的结构体对象。随后以
`[object, arguments...]` 作为完整实参序列，从翻译单元根作用域中名称为 `operator()`
的有效普通函数选择候选。对象匹配首形参，源码括号内的实参依次匹配其余形参；所有形参
共同使用普通调用的引用绑定、隐式转换、非平凡对象形成和逐形参重载排序规则。调用结果
由选中函数的返回类型按普通调用规则形成。对象与其余实参共同组成完整实参列表，运行时按普通调用所选 ABI 的方向求值。

对象调用不加入内建候选或虚接口声明，也不进行失败回退。调用目标若是普通函数名称、类型、
普通函数指针或虚接口指针，则只使用其原有直接调用、构造或间接调用语义；这些路径失败后
不再尝试 `operator()`。不存在有效 `operator()`、没有可行候选或调用歧义分别属于该对象
调用自身的错误。

`operator ()(object, arguments)` 可以显式调用同一组普通运算符函数，并继续形成普通直接
函数调用。`&operator ()` 可以取得函数地址，由直接语境提供的普通函数指针目标类型执行
完整函数类型的唯一精确选择。裸 `operator ()` 不是可独立使用的值。

原始结构体指针不会自动解引用：`pointer(arguments)` 仍按指针对象不可调用处理，调用其
pointee 必须显式写为 `(*pointer)(arguments)`。括号不改变已形成结构体对象的调用能力，
因此 `(object)(arguments)` 合法。调用目标对象属于普通对象读取，尚未完成初始化时不能调用。
`nonvirtual` 不修饰结构体对象调用。

### 引用绑定

下表同时适用于形参绑定、`mut T` 变量初始化，以及引用返回对象通过 `:=`、尾表达式或
`return expression;` 完成的初始化。引用绑定根据实参的表达式类型和值类别直接完成，
最终绑定由所在的初始化位置完成。转换的终点分别为 `mut T` 所需的 `T` 左值、
`copy T` 接受的 `const T` 左值或移动左值，以及 `move T` 所需的 `T` 移动左值。
Semantic AST 用 `ImplicitCastExpr <NoOp>` 表示同一对象的只读投影；它不读取值、
不形成对象、不调整地址，只增加顶层 const 并保留来源值类别；子表达式也保留原有类型
和值类别。已有 `const T` 左值或移动左值不增加此节点。纯右值仍先物化为移动左值，再按
需要增加 const。因此绑定 `copy T` 不要求把来源归一为左值。具名 `copy T` 引用的访问
以及返回 `copy T` 的调用结果仍为 const 左值。
表中的普通 `T` 目标也适用于 `const T` 普通对象初始化：先按 `T` 的规则构造对象，
完成后再施加顶层 `const`。

引用只能直接绑定到同型对象或该对象中唯一的基类子对象。设目标 referent 类型为 `T`，
源表达式去除当前层顶层 `const` 后的对象类型为 `S`：若 `S` 与 `T` 相同，则进行精确绑定；
若 `S` 和 `T` 均为结构体且 `S` 派生自 `T`，则沿静态继承路径选择源对象中的 `T` 基类
子对象并绑定。其它类型关系不能直接绑定。所有嵌套限定仍参与完整类型比较，绑定前不得先
执行数值、指针或用户定义隐式转换。

尚未形成对象的 `null` 使用目标驱动规则：从值目标 `P` 或引用目标取得具体指针类型
`P`，先形成 `P` 空值纯右值，再按下表绑定。因而可向 `copy P`、`move P` 提供物化后
的指针临时对象，不能绑定 `mut P`。引用绑定的是实际指针对象，不是其所指对象，不产生
空引用。此规则不放宽引用类型的使用位置限制，也不允许普通对象任意转换后再绑定引用。

| 实参 | `T` | `mut T` | `copy T` | `move T` |
|---|---|---|---|---|
| `T` 左值 | 按普通对象初始化规则 | 绑定 | 绑定 | 不允许 |
| `const T` 左值 | 按普通对象初始化规则 | 不允许 | 绑定 | 不允许 |
| `T` 移动左值 | 按普通对象初始化规则 | 不允许 | 绑定 | 绑定 |
| `const T` 移动左值 | 按普通对象初始化规则 | 不允许 | 绑定 | 不允许 |
| `T` 纯右值 | 按普通对象初始化规则 | 不允许 | 物化后绑定 | 物化后绑定 |

设 `D` 派生自 `B`，派生对象到基类引用的绑定如下：

| 实参 | 普通 `B` 对象 | `mut B` | `copy B` | `move B` |
|---|---|---|---|---|
| `D` 左值 | 从 `B` 左值读取或复制 | 绑定 `B` 子对象 | 绑定 `B` 子对象 | 不允许 |
| `const D` 左值 | 从 `const B` 左值读取或复制 | 不允许 | 绑定 `B` 子对象 | 不允许 |
| `D` 移动左值 | 从 `B` 移动左值读取、移动或复制回退 | 不允许 | 绑定 `B` 子对象 | 绑定 `B` 子对象 |
| `const D` 移动左值 | 从 `const B` 移动左值读取或复制 | 不允许 | 绑定 `B` 子对象 | 不允许 |
| `D` 纯右值 | 物化完整 `D`，再从 `B` 移动左值形成结果 | 不允许 | 物化完整 `D` 后绑定 `B` 子对象 | 物化完整 `D` 后绑定 `B` 子对象 |

绑定 `mut T`、`copy T` 或 `move T` 都不执行复制或移动。派生到基类绑定也不形成新的基类
对象，不改变完整派生对象或基类子对象的身份与生命周期。真正的复制或移动仍发生在普通
对象的初始化或赋值中。显式写出 `derived.Base` 后，来源表达式本身已经是 `Base` 子对象，
可继续按普通 `Base` 对象规则使用。

### 引用绑定的重载排序

引用绑定先比较目标对象类型，再比较同一目标上的引用模式：

1. 同型精确绑定优于任意派生到基类绑定。
2. 两个基类目标不同时，较接近源类型的基类绑定更优。例如 `Derived : Middle : Base` 中，
   到 `Middle` 的绑定优于到 `Base` 的绑定。
3. 目标对象类型相同时，按下表比较引用模式。

| 实参 | 可行引用形参 | 优先级 |
|---|---|---|
| `T` 左值 | `mut T`、`copy T` | `mut T` 优于 `copy T` |
| `const T` 左值 | `copy T` | 只有 `copy T` 可行 |
| `T` 移动左值 | `move T`、`copy T` | `move T` 优于 `copy T` |
| `const T` 移动左值 | `copy T` | 只有 `copy T` 可行 |
| `T` 纯右值 | `move T`、`copy T` | `move T` 优于 `copy T` |

该排序适用于普通调用的每个实参，也适用于接收者调用中接收者对首形参的绑定。每项
引用绑定的比较结果与其他实参转换一起参与“重载声明与决议”规定的逐参数支配比较。

同型普通 `T` 形参的精确值转换与同型引用绑定不可区分；若全部实参位置均不能区分，调用
歧义。`i32` 左值对 `copy i32` 的精确绑定优于到按值 `i64` 的数值转换；`*T` 左值对
`copy *T` 的匹配优于到按值 `*const T` 的内部限定转换。

基类路径的比较同时适用于按值和引用目标：同型匹配优于基类投影，同一继承链上较近的
目标优于较远的目标。只有双方均为引用、目标对象类型及基类路径相同时，才进一步比较
引用模式；同一基类上的按值形成与引用绑定不会仅因是否复制而分出优劣。

### 接收者调用

点接收者调用写为 `receiver.name(arguments)`。任意对象表达式都可以作为点接收者；
该形式是普通函数调用的另一种源码形式，不会使普通函数归属于接收者类型。

原始指针还可以使用箭头接收者调用 `pointer->name(arguments)`。点形式
`pointer.name(arguments)` 以指针对象本身作为接收者；箭头形式以原始指针的直接
pointee 左值作为接收者。箭头形式要求 pointee 是对象类型，其可修改性由 pointee 的
`const` 限定决定。原始指针只解引用一次，不对 pointee 继续执行自定义箭头递归。
例如 `*Derived` 箭头接收者在候选比较前就准备为实际的 `Derived` 解引用左值；选中
接受 `copy Base` 的候选后，再投影 `Base` 子对象并形成只读访问。
Semantic AST 的 `Receiver` 保存这一对象表达式：必要的指针读取在解引用内部，基类投影
和只读投影在解引用外部。消费者按该表达式求值，不根据保留的 `->` 语法标记再次解引用。
候选匹配或选后对象形成失败时，也保留已经完成的接收者来源准备。

字段选择与接收者调用由表达式形式直接区分：`object.name` 和 `pointer->name` 是字段
访问；没有在字段选择后插入分组括号的 `object.name(arguments)` 和
`pointer->name(arguments)` 是接收者调用。同名字段与普通函数不参与同一次选择，也不
在一方失败后回退到另一方。`(object.name)(arguments)` 和
`(pointer->name)(arguments)` 表示调用字段值，不解释为接收者调用。

普通具名接收者作为完整实参列表中的第一项，首形参可以是普通值类型，也可以是引用类型。
在相同候选范围与调用模式下，下列形式使用相同的参数匹配、转换比较和对象形成规则：

| 接收者调用 | 对应普通调用 |
|---|---|
| `x.F(arguments)` | `F(x, arguments)` |
| `p.F(arguments)` | `F(p, arguments)` |
| `p->F(arguments)` | `F(*p, arguments)` |

因此，`i32` 接收者可以传给按值 `i32` 形参，指针对象可以传给按值指针形参；派生对象也可以
按既有规则投影基类子对象，再形成按值基类形参。接收者不会隐式取地址，也没有引用形参
优先于按值形参的特殊规则。同型按值与引用匹配不可区分时，仍由其它实参决胜；没有唯一
胜者则调用歧义。

按值接收者复用读取、复制、移动、复制回退及同型纯右值直接传递规则。引用接收者复用
“引用绑定”及“引用绑定的重载排序”；物化和引用绑定本身不复制或移动对象。接收者只求值
一次，其转换与其余实参共同参与逐参数比较，接收者位置不具有全局优先权。

首形参名称不进入函数类型或重载签名。普通标识符与 `this` 在类型相同时具有相同匹配结果，
不能作为两个重载并存；显式 `this` 形参仍须满足其独立的引用类型声明规则，虚接口与
`operator()` 的声明约束也保持不变。

具名 `copy T` 引用产生 `const T` 左值，可以按值传递或绑定 `copy T`。具名 `move T`
引用产生 `T` 左值；仅比较引用候选时优先使用 `mut T`，再次显式写出 `move value` 后才
优先使用 `move T`。加入按值候选时，仍按普通调用规则共同比较。

唯一函数选定后，全部实参（含接收者）的对象形成都成功才检查转换风险并提交转换。
接收者形成失败时，在接收者位置报告错误，保留已选函数与准备后的来源；其它实参也不
提交转换，不回退到其它候选。

### 显式非虚具名调用

`nonvirtual` 可以紧邻函数名修饰当前一次具名函数调用：

```cw
nonvirtual F(arguments)
object.nonvirtual F(arguments)
pointer->nonvirtual F(arguments)
```

它只改变本次调用从同名函数符号形成候选声明的方式，不改变名称查找、实参求值、候选
可行性、转换序列或重载排序。三种形式都只执行一次重载决议；点号与箭头形式仍按“接收者
调用”规则将接收者作为首实参。

| 符号中的声明 | 常规调用 | `nonvirtual` 调用 |
|---|---|---|
| 未关联虚接口的普通函数 | 保留普通函数 | 保留普通函数 |
| 已关联虚接口的普通函数定义 | 以对应虚接口参与 | 保留普通函数定义 |
| 虚接口声明 | 保留虚接口 | 排除 |

因此，`nonvirtual` 调用可以选中普通函数或虚接口的类型外定义，选中后直接以该普通函数
声明作为被调函数；它不要求最终函数必须实际关联虚接口。抽象虚接口具有合法关联定义时，
该定义也可由 `nonvirtual` 调用选中，但接口本身及所属虚槽仍保持抽象；没有关联定义的抽象
接口不形成非虚候选。

`nonvirtual` 不是通用表达式前缀，也不是声明或函数类型修饰符。它不能用于函数指针、
虚接口指针等间接调用、构造调用、析构操作或运算符语法。基类子对象投影与调用模式相互
独立：
`object.Base.nonvirtual F(arguments)` 先选择 `Base` 子对象作为接收者，再以非虚模式形成
本次调用的候选。

### 函数返回块

非 `void` 函数的返回对象最初未初始化，包括平凡空结构体。返回对象可以通过尾表达式、
对具名返回对象显式初始化，或 `return expression;` 完成；对已经完整的返回对象再次
执行初始化属于重复初始化。空平凡类型可以用 `T()` 形成返回对象。

返回对象是本次函数调用的结果，其生命周期不因被调函数退出而结束。函数正常完成后，
已经初始化的返回对象继续作为调用表达式的结果存在。返回普通 `T` 的调用表达式仍是
纯右值；只有发生物化时，该表达式才直接指代它的结果对象。

尾表达式写入返回对象后正常结束函数。`return expression;` 先将表达式写入返回对象，
再立即退出函数；`return;` 只退出函数，不执行写入。自然到达函数体末尾与执行空
`return;` 时，非 `void` 返回对象必须已经初始化。

函数返回块末尾的最后一个 `if` 可以由各分支继续完成同一个返回对象；每条正常完成
函数的路径都必须完成返回对象。执行 `return` 的分支直接退出，不参与之后的控制流
汇合。

尾表达式和 `return expression;` 固定初始化返回对象。若返回对象此前已经初始化，
则诊断重复初始化；两者不会隐式改为赋值，也不会覆盖已有返回对象。对具名返回对象
显式使用 `:=` 或 `=` 时，分别遵守初始化和赋值的固定语义。

返回表达式先独立定型，再按普通对象初始化或引用绑定规则写入函数返回对象。函数返回
类型不会反向改变整数字面量的默认物化。

`void` 函数没有返回对象，可以自然结束或使用 `return;`，不得使用返回表达式或函数
尾值。

### 调用结果与引用返回

下表中的 `T` 表示没有顶层 `const` 的普通对象类型；普通按值返回顶层 `const T` 非法。

| 返回类型 | 调用表达式类型 | 值类别 |
|---|---|---|
| `T` | `T` | 纯右值 |
| `mut T` | `T` | 左值 |
| `copy T` | `const T` | 左值 |
| `move T` | `T` | 移动左值 |

返回 `void` 的调用具有 `void` 类型但没有对象值类别。

引用返回对象通过 `:=`、尾表达式或 `return expression;` 初始化时建立引用绑定。返回
引用必须指向在调用结果使用期间仍然有效的 referent；具体生命周期与逃逸检查规则另行
规定。

### 运算符函数

运算符函数是顶层函数，使用 `func operator` 声明：

```cw
func operator+(left Vector, right Vector) Vector {
  // ...
}
```

本文定义以下运算符函数：

- 一元 `+`、`-`、`!`，必须具有一个形参。
- 二元 `+`、`-`、`*`、`/`、`%`、`<`、`<=`、`>`、`>=`、`==`、`!=`、`=`，必须具有
  两个形参。
- 调用 `()`，必须至少具有一个形参，且第一形参必须是引用结构体的 `mut T`、`copy T`
  或 `move T`。其余形参数量不限。

上述运算符函数的至少一个形参必须是结构体类型，或者是以 `mut`、`copy`、`move`
引用结构体的类型。指向结构体的原始指针不满足该条件。返回类型沿用普通函数规则；
比较运算符函数也不强制返回 `bool`，`=` 也不强制返回 `void` 或 `mut T`。形参数量
不符或不含合格结构体形参的声明无效，不参与运算符表达式的重载选择。

恰好两个形参为 `dst mut T` 与 `src copy T` 的 `operator=` 是复制赋值；恰好为
`dst mut T` 与 `src move T` 的是移动赋值。身份只看这两个形参类型。不得声明同型
`func operator=(dst mut T, src mut T)`。习惯上复制赋值和移动赋值返回 `mut T` 并绑定
`dst`，但语言不强制。

用户可以使用 `operator op(arguments)` 显式调用运算符函数，例如
`operator -(value)`、`operator +(left, right)` 或 `operator ()(object, arguments)`。该形式
只从同一运算符名称的有效用户运算符函数中选择候选，不加入内建运算符，也不在用户候选
失败时回退到内建运算。实参数量、
参数适配、重载排序和调用结果均沿用普通直接函数调用规则；同名的一元和二元运算符函数由
普通重载决议自然区分。`operator op` 不是可独立使用的值，必须直接带有实参列表。

显式 `operator =(dst, src)` 是普通函数调用，不是 `dst = src` 的另一种赋值语法。它不会把
首实参视为写入目标，也不采用赋值表达式的右端优先求值规则；所有实参均须按照普通调用规则
在使用前完成初始化。

除调用 `()` 外，一元或二元运算符表达式至少含有一个结构体对象操作数时，从翻译单元顶层
按运算符拼写收集形参数量相同的用户函数候选，并按普通函数规则执行重载选择。当前不将
内建运算作为候选加入这一决议。不含结构体对象操作数时只使用内建运算规则；结构体原始
指针也不会触发运算符函数查找。调用 `()` 按“结构体可调用对象”一节处理，不加入内建候选。同型结构体 `=` 按“复制、移动与简单
赋值”选择复制赋值、移动赋值或内建按位写入；其它 `operator=` 重载是否参与 `=` 表达式
另行规定。

源码操作数按书写顺序对应普通函数实参，使用普通调用的引用绑定、隐式转换、逐形参重载
排序和选后对象形成规则。必须选出唯一最佳候选；否则表达式错误。运算符函数已经选定、但
形参对象形成失败时，不尝试其它候选，也不产生结果值。成功时表达式的类型和值类别由该
函数的返回类型按普通调用规则确定。除 `=` 外，操作数的运行时求值顺序与所选 ABI 下的
普通函数实参相同。`lhs = rhs` 始终先求值右端、再求值左端、再执行写入或调用。

运算符函数使用普通函数的重载签名规则。声明运算符函数不能创建新的运算符拼写，也不
能改变既有运算符的优先级或结合性。`:=` 表示对象初始化，不是可重载运算符。

## 语句

### 复合语句与表达式语句

复合语句是由 `{` 和 `}` 包围的语句序列，并建立嵌套词法作用域。表达式后接 `;`
形成表达式语句，表达式结果被丢弃。

普通复合语句不产生值，因此不得具有尾表达式。只有变量初始化块和函数返回块具有本文
定义的尾表达式语义。

### 条件语句

条件语句写为：

```cw
if condition {
  // ...
} else {
  // ...
}
```

`else` 可以省略。条件必须具有 `bool` 类型。位于结果位置的最后一个 `if` 按结果转发
规则处理。

### 循环语句

`while` 在每轮迭代前求值 `bool` 条件：

```cw
while condition {
  // ...
}
```

`break;` 结束最内层循环，`continue;` 开始最内层循环的下一轮。循环不产生尾部结果。

### 返回语句

返回语句为 `return;` 或 `return expression;`。

返回语句只能在函数体中使用。变量初始化块中不得使用返回语句。

## 结构体与对象

### 结构体与字段

结构体声明写为：

```cw
struct Name {
  field Type;
}
```

同一结构体的直接字段名称必须唯一。内建对象、结构体、固定数组、原始指针、函数指针和虚接口指针
可以作为字段；`void`、裸函数类型和引用不能作为字段。普通对象字段是其声明类型的子对象。字段
随所属结构体对象一起结束生命周期；平凡结构体中，字段的生命周期从该字段初始化完成时
开始。

没有字段且没有基类的平凡结构体可以声明和使用；它自身具有一个初始化状态叶子，
例如通过 `Empty()` 实际形成。非平凡空结构体仍必须通过构造函数形成。

### 平凡类型与非平凡类型

每个完整对象类型要么平凡，要么非平凡。内建对象类型、原始指针、函数指针和虚接口指针是
平凡对象类型。`[N] T` 的平凡性与 `T` 完全相同；包括 `N` 为零时，长度都不改变这一分类。

`trivial struct T` 显式声明平凡结构体；普通 `struct T` 显式声明非平凡结构体，即使
没有基类、字段或特殊函数也不自动推导为平凡。编译器验证 `trivial` 类别声明，
违反以下条件时诊断，不自动改判为非平凡：

- 没有用户声明的构造函数或析构函数；
- 没有复制赋值或移动赋值；
- 没有虚函数；
- 所有直接基类都是平凡结构体；
- 所有字段的类型都是平凡对象类型。

普通非平凡结构体必须声明至少一个构造函数，以及恰好一个析构函数。平凡结构体不得
声明构造、析构、复制赋值或移动赋值函数。其它 `operator=` 重载、普通函数及接收者
调用不改变平凡分类。`trivial` 是全局保留字，其声明修饰用途限定为 `trivial struct`。

平凡结构体的按值初始化、按值传参和同型简单赋值按位复制已经开始生命周期的对象表示。
平凡数组使用等价的普通对象路径，语言上按下标递增复制或赋值元素，允许等价的整块实现。
非平凡结构体的同型纯右值可以直接形成初始化目标、按值形参或按值返回对象，而不执行
复制或移动；同型左值通过显式复制构造形成这些对象，同型移动左值优先通过显式移动构造
形成，没有移动构造时退回复制构造。
非平凡结构体必须显式声明复制赋值或移动赋值后才能同型赋值；语言不因成员可赋值而隐式
生成这些函数。
非平凡数组没有用户可见或隐式合成的特殊函数；它按元素类型递归使用已确定的复制、
移动、赋值和析构语义。元素按下标递增形成或赋值，按下标递减析构；嵌套数组递归应用
相同顺序。

### 平凡结构体的默认初始化

合法平凡结构体 `T` 可以通过 `T()` 整体默认形成一个 `T` 纯右值。该操作不声明或合成
构造函数，不参与构造重载决议；无参数以外的调用仍须遵守普通构造规则。空结构体的
`T()` 同样具有实际对象形成的语义。同型纯右值继续直接形成最终目标。

```cw
trivial struct Value { number i32; pointer *Value; }
func Make() Value { Value() }
```

默认值由各组成类型直接定义：

| 类型 | 默认值 |
|---|---|
| 固定宽度整数、`isize`、`usize` | 本类型的数值零 |
| `f32`、`f64` | 正零 |
| `bool` | `false` |
| 原始指针、函数指针、虚接口指针 | 本类型的空值 |
| 平凡结构体 | 直接基类和字段递归取得默认值 |
| 平凡数组 | 每个元素递归取得默认值 |

默认空指针是已初始化的值，但不能用于访问对象或调用函数；引用没有空绑定。当前仅
平凡结构体提供直接的 `T()` 源码入口，内建类型、指针及数组的默认值用于组合对象，
不新增它们的独立默认初始化写法。无来源声明仍表示未初始化，字段不提供自定义默认值。
这些规则不规定全零字节表示，也不要求逐成员展开或使用 `memset`。

### 继承

基类声明使用冒号指定一个基类型：

```cw
struct Derived : Base {
  // ...
}
```

结构体的直接基类关系必须无环；结构体不得直接或间接继承自身。非平凡结构体不得继承
平凡结构体。平凡结构体的基类必须是平凡结构体。

字段与基类子对象名称查找都从基础表达式的静态结构体类型开始。每一层同时考虑该层的
直接字段和直接基类类型名；当前层没有目标名称时再沿直接基类继续查找。当前层找到的
名称隐藏更深继承层级中的同名字段或基类，声明这种隐藏不产生错误。

基础表达式后写出按上述规则查找到的基类名称，可以显式选择该基类子对象：

```cw
object.Base
pointer->Base
```

一次投影可以省略中间基类名称并选择无歧义的间接基类，例如 `object.Root`；沿真实子对象
路径连续写出的 `object.Middle.Root` 也合法。显式路径可用于绕过派生层名称隐藏。投影不
执行转换、复制、移动或构造，也不搜索运行时类型。若同一层的直接字段名称与直接基类类型
名称相同，结构体声明非法，以保证该层后缀没有字段与基类两种解释。

点号基类投影与字段访问采用相同的值类别和 `const` 传播规则：左值和移动左值基础分别
得到同类别的基类子对象；纯右值基础先物化完整对象，再得到基类子对象移动左值。箭头
投影把 pointee 作为左值基础，并保留 pointee 的只读限制。投影所得基类子对象仍具有独立
对象身份；普通使用必须先满足确定初始化规则。

引用绑定还可以根据目标 referent 类型隐式选择同一对象中的基类子对象。该选择沿结构体
声明身份确定的唯一静态继承路径进行，值类别与 `const` 传播和显式投影相同；读取只要求
被选基类子对象的范围已经初始化。隐式选择不执行名称查找，不受字段隐藏影响，也不形成
按值 `Base` 对象。对象引用绑定与原始指针的派生转换是两种独立适配：前者直接选择对象中的
基类子对象，后者调整一个指针值。

### 字段访问

字段名称不进入词法作用域。字段访问从基础表达式的静态结构体类型开始单独查找。

`object.field` 要求基础表达式具有结构体类型。尚未整体初始化完毕的具名平凡对象和构造
函数中的 `this` 仍可作为字段访问的基础。`pointer->field` 要求基础表达式去除
顶层 `const` 后是原始指针，且其被指向类型去除顶层 `const` 后是结构体类型；原始
指针只访问直接 pointee，不继续递归解释该对象。

使用 `object.field` 访问成功后，字段表达式沿用基础对象的值类别。左值基础对象的字段
是左值，移动左值基础对象的字段是移动左值；纯右值基础对象先物化，其字段是移动左值。
字段自身或基础对象的 const 限定不改变类别，因此 const 字段也可以产生 const 移动左值。

| 基础表达式 | 字段表达式 |
|---|---|
| 左值 | 左值 |
| 移动左值 | 移动左值 |
| 纯右值 | 先物化完整对象，再得到移动左值 |

使用 `pointer->field` 时，指针表达式只提供地址；pointee 作为左值基础对象访问，因此
字段表达式始终是左值，不继承指针表达式的值类别或指针自身的顶层 `const`。

字段类型同时传播基础对象的 `const` 访问限制。通过 `const T` 对象或指向 `const T`
的指针访问字段时，所得字段表达式具有相应的 `const` 限定；只读权限不通过值类别表达。

### 构造函数与析构函数

构造函数和析构函数分别写为：

```cw
ctor T(parameters) {
  // ...
}

dtor T() {
  // ...
}
```

`ctor` 和 `dtor` 标识声明种类。`T` 是构造函数或析构函数的名称，同时表示
被构造或析构的类型。同一类型可以声明多个显式形参类型不同的构造函数。非平凡类型恰好
声明一个析构函数；平凡类型不得声明构造函数或析构函数。没有隐式生成的可调用构造函数
或析构函数。

构造函数的形参使用普通函数接口规则。精确形态 `ctor T(other copy T)` 和
`ctor T(other move T)` 分别具有复制构造和移动构造身份，并按前述对象形成规则从同型
左值或移动左值隐式选择。构造与赋值互相独立：复制或移动构造产生尚未出生的新对象，
复制或移动赋值修改已经初始化的对象。

函数体通过保留字 `this` 指代正在构造或析构的对象。在构造函数和析构函数体中，`this`
的类型都是 `T`，值类别都是左值；它不是指针或形参，也不出现在函数类型或重载签名中。
其它上下文不得使用 `this`。
`T(arguments)` 与 `ctor (ptr) T(arguments)` 共用同一函数体，只是 `this` 指代的存储
不同。构造期间 `this` 可能尚未完整；析构入口的 `this` 已经完整且可修改。即使完整对象
具有 `const T` 类型，析构函数仍可通过 `this` 修改它。不得把尚未整体初始化完毕的
`this` 当作完整 `T` 使用。构造名称必须直接位于调用括号左侧；当 `T` 只表示类型时，
`(T)(arguments)` 不能恢复构造含义。只有下述直接以 `this` 为目标、以同型构造结果为来源
的初始化可以把整个 `this` 作为委托目标；来源外层的普通括号不改变这一点。
`this := other` 等任意来源的整对象复制或移动初始化非法。
具名 `move T` 形参仍是 `T` 左值；要继续移动必须再次写出 `move`。

构造函数体必须走下列两条路径之一，不得混合。委托或直接基类构造若存在，必须就是
函数体的第一条语句；局部声明、普通表达式或控制语句也不能位于它之前。

**委托**

函数体的第一条语句必须是 `this := T(arguments);` 或仅在来源外增加分组的
`this := (T(arguments));`，其中来源调用同一类型的另一个构造函数。该构造在当前 `this`
的结果位置执行，而不是先形成独立临时对象。目标构造函数
完整执行自行构造过程；返回后对象已经初始化完毕。本函数体不得再初始化基类或成员，
否则诊断重复初始化。此后只允许普通语句。脱离该目标位置的 `T(arguments)` 只是常规
完整对象构造，不形成委托。

构造函数不能直接或间接委托给自身。委托关系形成环时，环中的构造函数均无效；该委托
不能使 `this` 变为已初始化。

**自行构造**

必须完成：

1. 若存在直接基类，第一条语句必须精确以 `this.Base := source;` 一次性初始化 `this` 中
   完整的直接基类子对象。`source` 必须按普通对象形成规则形成 `Base`；常见写法是
   `this.Base := Base(arguments);`，也可以使用可用的复制构造、移动构造或同型纯右值直接
   传递。右侧显式构造在该基类结果位置执行，不得先形成独立完整 `Base` 临时对象；也不得
   通过对基类字段逐个 `:=` 来代替。当前不会隐式把派生对象适配成其中的基类子对象。
   没有基类则省略本步。
2. 按字段声明顺序使用 `this.field := ...` 初始化全部直接字段。平凡字段递归遵守相同
   顺序；非平凡
   字段必须写成 `this.field := Field(arguments)`，把构造结果直接放入该字段。
3. 在字段尚未全部完成期间可以执行普通计算、局部声明和控制流，但任何使用都仍受确定
   初始化规则约束。允许使用 `if` 使各分支完成同一组字段；循环不得承担这项职责。
4. 每条能够正常离开构造函数的路径都必须使基类与全部字段完整。对象完整后，字段只能
   赋值，不能再次 `:=`。

析构函数体执行期间，`this` 表示完整且可修改的对象。正常退出时先完成函数体局部
清理，再按字段声明逆序析构直接字段，最后析构直接基类；`return;` 同样执行这些步骤。
部分构造失败与异常展开的规则另行规定。

构造函数和析构函数有以下使用形式：

| 形式 | 作用 | 结果 |
|---|---|---|
| `T(arguments)` | 常规构造 | `T` 纯右值 |
| `ctor (ptr) T(arguments)` | 在指定地址调用构造函数 | `*T` 纯右值 |
| `dtor (ptr) T()` | 在指定地址调用析构函数 | `void` |

表中的构造函数调用与析构调用适用于非平凡类型。平凡结构体的无参数 `T()` 则采用
前述整体默认初始化规则，不调用构造函数。
平凡类型不得使用指定地址构造或指定地址析构。`dtor (ptr) T();` 是析构调用表达式后接
分号形成的普通表达式语句。

`ctor (ptr) T(arguments)` 和 `dtor (ptr) T()` 使用 `ptr` 指定调用目标
地址。`ptr` 不属于显式实参；构造函数根据 `arguments` 选择，`ptr` 不参与
构造函数重载决议。构造实参使用与普通函数调用相同的形参数量、引用绑定、隐式转换
和重载排序规则。

在当前地址类型边界内，指定地址构造要求 `ptr` 读取后具有精确的 `*T` 类型，
不接受 `*const T`；指定地址析构接受精确的 `*T` 或 `*const T`。这两种形式当前均不
接受 `*void`、其他对象类型的指针、派生类型指针或整数地址。未来可以在原始存储和
对象生命周期规则明确后扩展可接受的地址形式。

`ctor (ptr) T(arguments)` 产生地址与 `ptr` 指定地址相同的 `*T`。指定
地址构造不分配内存，指定地址析构不释放内存。构造函数成功完成后，位于该地址的对象
开始生命周期；析构完成后，该地址上的对象生命周期结束。

### 对象生命周期

对象或整体叶子通过初始化或构造开始生命周期。平凡结构体的叶子可以先于整体对象分别
完成初始化；普通非平凡对象和非平凡字段只能整体形成。数组作为整体初始化，元素按
下标递增形成、递减析构；零长度不改变数组作为一个对象的身份。

#### 初始化顺序

同一词法作用域中，开始初始化后声明的值对象时，之前声明的值对象必须已经完整。
整数、指针、平凡类、空对象和数组均参与；引用绑定不参与这条队列。当前翻译单元中的
全局变量以统一外层作用域应用同一规则。不同作用域独立排序，在嵌套语句中初始化外层
对象仍按该对象所属作用域判断。初始化对象的任一可独立初始化子对象，也属于开始
初始化该对象。

同一对象内部先初始化直接基类，再按声明顺序初始化字段；平凡子对象递归应用此规则。
前一个子对象完整后才能开始后一个，不能通过深层字段投影绕过限制。数组及非平凡
子对象仍作为整体边界。构造函数的局部变量队列与 `this` 的子对象队列相互独立，
委托或直接基类构造仍须满足已有的前导语句要求。

#### 正常清理责任与顺序

自动管理对象在所属边界退出时由编译器清理。局部值对象按声明逆序销毁，只清理已成功
形成的对象；自然结束、`return`、`break` 和 `continue` 均处理实际退出的作用域。
非平凡结构体先执行用户析构函数体并清理体内局部对象，再逆序析构直接字段，最后
析构直接基类。平凡对象不调用不存在的析构函数。

指定地址构造的独立对象由程序管理，即使存储位于栈上也不自动登记作用域清理。显式
析构与自动析构采用相同的类型析构规则；通过指针显式调用 `dtor` 不撤销原有自动清理
责任，编译器不追踪原始指针别名来改变该责任。

函数返回对象不因被调函数退出销毁，初始化块结果继续归属于外层变量。同型纯右值直接
形成目标，不增加重复临时对象或清理。`move` 保持来源对象存活且已初始化。
全局对象在全局初始化完成后继续存活，在后续全局清理阶段按初始化逆序销毁；实际入口
由 Generator 后续实现。各作用域分别逆序清理，退出嵌套作用域时先内层、后外层，
不要求跨作用域的全部对象遵循一条初始化逆序时间线。

#### 调用顺序与按值形参

编译上下文统一选择 `Itanium` 或 `Microsoft`，默认 `Itanium`。一个实参求值并完成
对应形参形成后才处理下一个；处理方向不改变源码中实参与形参的对应位置。接收者调用
和可调用对象调用的接收者是完整实参序列第一项。已有明确规则优先，`lhs = rhs` 始终
先右后左，数组元素及多项直接初始化仍从左到右。

| 配置 | 实参求值及形参形成方向 | 按值形参的正常析构 |
|---|---|---|
| Itanium | 从左到右 | 调用方外围完整表达式结束时，按实际成功形成的逆序 |
| Microsoft | 从右到左 | 被调函数退出并清理函数体局部对象后，按形参声明顺序从左到右 |

这里的 Itanium 从左到右是 CW 的选择，不是 Itanium ABI 的强制规定。引用形参只结束
绑定，不销毁 referent。同型纯右值直接形成形参对象，只形成和清理一次。完整 ABI 的
布局及链接规则另行规定。

#### 完整表达式边界

| 语境 | 清理时机 |
|---|---|
| 表达式语句 `E;` | 整个表达式完成后 |
| 局部直接声明初始化的每一项 | 对应变量形成后、下一项开始前 |
| 初始化块尾表达式列表的每一项 | 对应结果形成后、下一尾项开始前 |
| `if`、`while` 条件 | 条件值取得后、进入分支或执行退出后续步骤前 |
| `return E;`、函数尾表达式 | 返回对象形成或返回引用绑定后、局部清理前 |

同一完整表达式内的普通临时对象和 Itanium 按值形参统一按实际成功形成的逆序清理，
只处理实际执行路径。Microsoft 按值形参由被调函数处理，不重复清理。被丢弃的非平凡
纯右值也须清理；直接形成最终目标的结果不再作为额外临时对象清理。

括号、嵌套调用、条件表达式及短路子表达式不建立额外的调用方完整表达式边界。被调
函数体及初始化块内部的语句按自身语境建立边界，不延后至整个调用或初始化块结束。
无来源声明、空 `return`、`break`、`continue` 不增加来源表达式边界，但仍正常退出
作用域。当前不引入引用延长临时生命周期、异常展开或构造失败时的部分对象清理规则。

### 虚函数声明

结构体可以在连续的 `virtual` 块中声明虚接口。虚接口只包含函数签名，不包含函数体。
`override` 和 `abstract` 放在 `func` 前；两者同时出现时顺序不限。后置形式是普通
语法错误，不保留兼容语法：

```cw
struct Base {
  virtual {
    func Draw(this mut Base) void;
    abstract func Measure(receiver copy Base) i32;
  }
}

func Draw(object mut Base) void {
}

ctor Base() {
}

dtor Base() {
}
```

四种合法形式为：

| 形式 | 含义 |
|---|---|
| 无修饰 | 引入具体虚接口 |
| `abstract` | 引入抽象虚接口 |
| `override` | 具体覆写继承虚接口 |
| 同时含 `override` 与 `abstract` | 将继承虚接口重新抽象化 |

`override abstract` 与 `abstract override` 含义相同。每个修饰词最多出现一次；重复修饰非法。
`abstract` 与 `override` 是全局保留字。

虚接口必须具有函数名称和具名第一形参。第一形参必须精确为 `mut T`、`copy T` 或
`move T`，其中 `T` 是包含该 `virtual` 块的结构体；它可以命名为 `this`，也可以使用
普通标识符。该形参是进入函数类型和实参序列的真实引用形参，不是构造函数或析构函数
中的隐式目标对象。其名称与其余形参名称一样，只在各自声明或函数体内有效，不进入函数
类型、重载身份、实现关联或覆写匹配。

同一虚接口内的形参名称必须互不相同，具名返回对象也不得与形参重名。

具体虚接口必须具有一个定义。该定义写在结构体外，仍使用普通函数定义语法；它按函数名称
和完整形参类型与虚接口关联，其中包括第一形参的引用模式和 referent 类型。找到同一声明
身份后，返回类型还必须精确相同。关联与源码先后无关。关联成功的函数定义只提供该虚接口
的实现体；常规调用以虚接口而不是该普通函数定义参与候选形成，显式 `nonvirtual` 调用则
以普通函数定义参与并排除虚接口。抽象虚接口可以没有定义，也可以具有一个定义；即使具有
定义，接口仍保持抽象。

覆写匹配使用函数名称、第一形参的引用模式以及其余形参类型；第一形参 referent 分别是
当前基类型和派生类型，不要求字面相同。返回类型不参与虚槽匹配。找到继承虚槽后，覆写
返回类型必须与被覆写返回类型完全相同，或者满足下述协变返回规则。改变继承虚槽必须显式
写出 `override`；无继承目标的 `override` 非法。同时含 `override` 与 `abstract` 的声明
可以覆写具体或抽象接口，后续派生类型可以再次使用具体 `override`。

协变返回只允许一层结构体对象指针或结构体引用。设覆写接口返回的对象类型为 `Derived`，
被覆写接口返回的对象类型为 `Base`，则 `Derived` 必须等于 `Base`，或沿唯一静态继承路径
派生自 `Base`。返回对象的继承层级与虚接口接收者的继承层级相互独立；返回对象不要求
属于接收者类型的继承层级。

指针返回还必须保留被覆写接口要求的直接 pointee `const`：`*Derived` 可以覆写
`*Base` 或 `*const Base`，`*const Derived` 只能覆写 `*const Base`。多层指针不协变，
因此 `**Derived` 不能覆写 `**Base`。

引用返回按下表检查；表中的 `Derived` 也可以与 `Base` 是同一类型：

| 被覆写接口返回 | 合法覆写返回 |
|---|---|
| `mut Base` | `mut Base`、`mut Derived` |
| `copy Base` | `copy Base`、`copy Derived`、`mut Base`、`mut Derived` |
| `move Base` | `move Base`、`move Derived` |

`mut` 与 `copy` 属于左值引用类别，`move` 属于移动引用类别；两类当前不得跨类别协变。引用
协变不构造临时对象，不执行复制、移动、按值切片或用户定义转换。具体覆写、抽象覆写与重新
抽象都使用同一套返回规则。非法返回类型不会替换继承虚槽。

调用表达式的结果始终由静态选中的虚接口声明决定。若静态选中的接口返回 `*Base`，
调用结果就是 `*Base`；只有静态选中声明返回 `*Derived` 的覆写接口时，调用结果才是
`*Derived`。运行时最终覆写的更具体返回类型不进入调用点的静态类型。类型外函数实现
本地虚接口时不使用协变，其返回类型仍必须与该本地接口完全相同。

派生类型只有在自身 `virtual` 块中显式声明本地 `override` 后，才改变继承虚槽。仅在
结构体外定义一个以派生类型为第一形参、签名与继承槽相符的普通函数，会在派生类型声明处
诊断缺少显式覆写；该函数本身仍是普通函数，继续参与普通重载决议并正常分析函数体。

虚接口在词法上属于结构体，但其函数名称与普通函数一起进入翻译单元根函数重载集合，
并服从相同的类型、函数和全局变量名称冲突规则。直接调用和接收者调用使用同一候选可行性、
隐式转换与重载排序。静态选中虚接口时，调用身份是该虚接口；动态类型不参与重载决议，
运行时最终覆写者由后续虚派发阶段选择。虚接口的抽象性、定义是否存在以及覆写链不改变
候选排序。

继承虚接口不建立一套特殊的接收者转换规则。`func F(this copy Base)` 与普通函数一样使用
统一引用绑定，因此 `Derived` 对象可以隐式选择其 `Base` 子对象并调用继承接口；只有要改变
该虚槽时，派生类型才必须声明本地 `override`。静态选中基类接口不关闭虚派发，接收者的
引用绑定不执行按值切片；普通原始指针值仍只按照“指针类型”一节执行已经规定的安全转换。

若一个结构体的最终虚槽中至少有一个仍为抽象接口，该结构体是抽象结构体。抽象结构体可以
作为基类，也可以通过 `mut`、`copy`、`move` 引用或原始指针使用，并可以从这些静态类型
发起虚调用；但不得形成抽象结构体的完整对象。因此抽象结构体不得作为字段、普通按值形参
或普通按值返回类型，不得作为固定数组元素，不得声明完整对象变量，也不得通过 `T(arguments)` 或指定地址构造
形成完整对象。抽象结构体仍可声明构造函数和析构函数，并可在派生结构体构造期间作为基类
子对象被构造或参与同型委托。
